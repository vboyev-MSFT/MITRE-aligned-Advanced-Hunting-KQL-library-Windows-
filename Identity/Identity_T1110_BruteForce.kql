// Domain: Identity | Technique: T1110 Brute Force (burst of failures)
IdentityLogonEvents
| where ActionType == "LogonFailed"
| summarize Failures=count() by AccountUpn, DeviceName, bin(Timestamp, 5m)
| where Failures >= 20
